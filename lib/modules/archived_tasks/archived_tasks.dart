import 'package:flutter/material.dart';

class ArchivedTasksPage extends StatelessWidget {
  const ArchivedTasksPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Archived Tasks'),
    );
  }
}
